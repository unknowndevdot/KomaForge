<Window x:Class="KomaForge.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="KomaForge - Comic Layout"
        Width="1280"
        Height="820"
        MinWidth="980"
        MinHeight="640"
        Background="{StaticResource WindowBackgroundBrush}">
    <DockPanel>
        <Menu DockPanel.Dock="Top" Background="#F3F0EA">
            <MenuItem Header="파일(_F)">
                <MenuItem x:Name="NewMenuItem" Header="새로 만들기" InputGestureText="Ctrl+N" Click="NewProject_Click" />
                <MenuItem x:Name="OpenMenuItem" Header="불러오기..." InputGestureText="Ctrl+O" Click="LoadProject_Click" />
                <MenuItem x:Name="SaveMenuItem" Header="저장" InputGestureText="Ctrl+S" Click="SaveMenuItem_Click" />
                <MenuItem Header="다른 이름으로 저장..." Click="SaveProject_Click" />
                <Separator />
                <MenuItem Header="전체 페이지 내보내기..." Click="ExportAllPages_Click" />
                <MenuItem Header="현재 페이지 내보내기..." Click="ExportCurrentPage_Click" />
                <Separator />
                <MenuItem x:Name="PreferencesMenuItem" Header="환경설정..." InputGestureText="Ctrl+," Click="Preferences_Click" />
                <Separator />
                <MenuItem Header="종료" Click="ExitMenuItem_Click" />
            </MenuItem>
            <MenuItem Header="편집(_E)">
                <MenuItem x:Name="UndoMenuItem" Header="실행취소" InputGestureText="Ctrl+Z" Click="Undo_Click" />
                <MenuItem x:Name="RedoMenuItem" Header="다시실행" InputGestureText="Ctrl+Y" Click="Redo_Click" />
                <Separator />
                <MenuItem x:Name="CutMenuItem" Header="잘라내기" InputGestureText="Ctrl+X" Click="Cut_Click" />
                <MenuItem x:Name="CopyMenuItem" Header="복사" InputGestureText="Ctrl+C" Click="Copy_Click" />
                <MenuItem x:Name="PasteMenuItem" Header="붙여넣기" InputGestureText="Ctrl+V" Click="Paste_Click" />
            </MenuItem>
            <MenuItem Header="보기(_V)">
                <MenuItem x:Name="PageFitMenuItem"
                          Header="페이지 쪽 맞춤"
                          IsCheckable="True"
                          Checked="PageFitCheckBox_Changed"
                          Unchecked="PageFitCheckBox_Changed" />
                <MenuItem x:Name="PageWidthFitMenuItem"
                          Header="페이지 폭 맞춤"
                          IsCheckable="True"
                          Checked="PageFitCheckBox_Changed"
                          Unchecked="PageFitCheckBox_Changed" />
            </MenuItem>
        </Menu>
        <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="InspectorColumn" Width="360" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border x:Name="InspectorPanel"
                Grid.Column="0"
                Background="#FFFFFF"
                BorderBrush="#DDD8CE"
                BorderThickness="0,0,1,0"
                Padding="16">
            <ScrollViewer x:Name="InspectorScrollViewer"
                          VerticalScrollBarVisibility="Auto"
                          PreviewMouseWheel="InspectorScrollViewer_PreviewMouseWheel">
                <StackPanel Margin="0,0,12,0">
                    <TextBlock Text="설명"
                               Foreground="#55514A"
                               Margin="0,0,0,6" />
                    <TextBox x:Name="ComicTitleTextBox"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Disabled"
                             Padding="6,2"
                             TextChanged="ComicTitleTextBox_TextChanged"
                             Margin="0,0,0,12" />

                    <!-- 비주얼 노벨 모드 토글은 인스펙터에 노출하지 않는다(단축키 Ctrl+T로 켜고 끔).
                         컨트롤은 섹션 표시/숨김 로직을 위해 그대로 두되 화면에는 보이지 않게 Collapsed. -->
                    <CheckBox x:Name="FlowTextModeCheckBox"
                              Content="비주얼 노벨 모드 (여러 페이지에 본문 흐름)"
                              Visibility="Collapsed"
                              Margin="0,0,0,12"
                              Checked="FlowTextModeCheckBox_Changed"
                              Unchecked="FlowTextModeCheckBox_Changed" />

                    <Border x:Name="VnSectionBorder" Style="{StaticResource InspectorSection}" Visibility="Collapsed">
                    <StackPanel>
                    <TextBlock Text="비주얼 노벨 생성" Style="{StaticResource SectionHeader}" />
                    <TextBlock Text="스크립트(한 줄 = 한 페이지). '이름: 대사' 형식, 이름 없는 줄은 서술로 처리됩니다."
                               TextWrapping="Wrap" FontSize="11" Foreground="#8A8479" Margin="0,0,0,6" />
                    <TextBox x:Name="VnScriptBox"
                             AcceptsReturn="True" AcceptsTab="True" TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto" Height="120"
                             Padding="6,4" Margin="0,0,0,8" />
                    <TextBlock Text="템플릿 페이지 (생성 시 복제할 페이지)" Foreground="#55514A" Margin="0,0,0,4" />
                    <ListBox x:Name="VnTemplateListBox"
                             MinHeight="56" MaxHeight="120"
                             DisplayMemberPath="Name"
                             SelectionChanged="VnTemplateListBox_SelectionChanged"
                             Margin="0,0,0,6" />
                    <UniformGrid Rows="1" Columns="2" Margin="0,0,0,8">
                        <Button Content="템플릿 복제" Click="VnAddTemplate_Click" Margin="0,0,4,0"
                                ToolTip="템플릿을 선택 중이면 그 템플릿을, 아니면 현재 일반 페이지를 복제해 템플릿 목록에 추가합니다." />
                        <Button Content="템플릿 삭제" Click="VnDeleteTemplate_Click" Margin="0" />
                    </UniformGrid>
                    <Button Content="생성" Click="VnGenerate_Click" Padding="8,5" Margin="0"
                            ToolTip="스크립트 각 줄마다 선택한 템플릿을 복제하고 '이름'/'서술' 말풍선을 줄 내용으로 교체해 페이지를 만듭니다." />
                    </StackPanel>
                    </Border>

                    <Border x:Name="PageSectionBorder"
                            Style="{StaticResource InspectorSection}"
                            IsKeyboardFocusWithinChanged="PageSection_FocusChanged">
                    <StackPanel>
                    <TextBlock Text="페이지" Style="{StaticResource SectionHeader}" />
                    <ListBox x:Name="PageListBox"
                             MinHeight="86"
                             MaxHeight="150"
                             SelectionChanged="PageListBox_SelectionChanged"
                             MouseDoubleClick="PageListBox_MouseDoubleClick"
                             PreviewKeyDown="PageListBox_PreviewKeyDown"
                             Margin="0,0,0,8">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <!-- 표시: "1. 이름" (번호는 항목 위치). 편집 중이면 숨김. -->
                                    <StackPanel Orientation="Horizontal">
                                        <StackPanel.Style>
                                            <Style TargetType="StackPanel">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <TextBlock Text="{Binding DisplayIndex}" />
                                        <TextBlock Text=". " />
                                        <!-- 비주얼 노벨 모드면 말풍선 텍스트 요약, 아니면 페이지 이름(DisplayLabel) -->
                                        <TextBlock Text="{Binding DisplayLabel}" TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                    <!-- 편집: 더블클릭/F2 시 표시되는 입력칸(기본 숨김은 Style로 — 로컬 값은 트리거를 덮어쓰므로). -->
                                    <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             Padding="2,0"
                                             IsVisibleChanged="PageRenameBox_IsVisibleChanged"
                                             KeyDown="PageRenameBox_KeyDown"
                                             LostFocus="PageRenameBox_LostFocus">
                                        <TextBox.Style>
                                            <Style TargetType="TextBox">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBox.Style>
                                    </TextBox>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <UniformGrid Rows="1" Columns="5" Margin="0,0,0,18">
                        <Button Content="추가"
                                Click="AddPage_Click"
                                Margin="0,0,4,0" />
                        <Button Content="복제"
                                Click="DuplicatePage_Click"
                                Margin="0,0,4,0" />
                        <Button Content="삭제"
                                Click="DeletePage_Click"
                                Margin="0,0,4,0" />
                        <Button Content="위로"
                                Click="MovePageUp_Click"
                                Margin="0,0,4,0" />
                        <Button Content="아래로"
                                Click="MovePageDown_Click"
                                Margin="0" />
                    </UniformGrid>

                    <!-- 페이지 리스트에 포커스가 있을 때만 보이는 하위 옵션 -->
                    <StackPanel x:Name="PageEditControls" Visibility="Collapsed">
                    <TextBlock Text="페이지 크기 (너비 × 높이)"
                               Foreground="#55514A"
                               Margin="0,0,0,6" />
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="PageWidthTextBox"
                                 Grid.Column="0"
                                 Text="832"
                                 Padding="6,2"
                                 VerticalContentAlignment="Center"
                                 TextChanged="PageSizeTextBox_TextChanged" />
                        <TextBlock Text="×"
                                   Grid.Column="1"
                                   VerticalAlignment="Center"
                                   Margin="6,0"
                                   Foreground="#55514A" />
                        <TextBox x:Name="PageHeightTextBox"
                                 Grid.Column="2"
                                 Text="1216"
                                 Padding="6,2"
                                 VerticalContentAlignment="Center"
                                 TextChanged="PageSizeTextBox_TextChanged" />
                    </Grid>
                    <StackPanel Orientation="Horizontal" Margin="0,2,0,14">
                        <TextBlock Text="배경색"
                                   Width="64"
                                   VerticalAlignment="Center"
                                   Foreground="#55514A" />
                        <ComboBox x:Name="PageBackgroundColorComboBox"
                                  Width="130"
                                  SelectionChanged="PageBackgroundColorComboBox_SelectionChanged" />
                    </StackPanel>

                    <TextBlock Text="칸 구성 / 여백 / 간격"
                               Foreground="#55514A"
                               Margin="0,0,0,6" />
                    <UniformGrid Rows="1" Columns="3" Margin="0,0,0,8">
                        <TextBox x:Name="LayoutPatternTextBox"
                                 Text="1,2,1"
                                 Padding="6,2"
                                 VerticalContentAlignment="Center"
                                 Margin="0,0,4,0"
                                 ToolTip="칸 구성 (예: 1,2,1)" />
                        <TextBox x:Name="AutoMarginTextBox"
                                 Text="24"
                                 Padding="6,2"
                                 VerticalContentAlignment="Center"
                                 Margin="0,0,4,0"
                                 ToolTip="자동 생성 여백" />
                        <TextBox x:Name="AutoGutterTextBox"
                                 Text="14"
                                 Padding="6,2"
                                 VerticalContentAlignment="Center"
                                 Margin="0"
                                 ToolTip="자동 생성 간격" />
                    </UniformGrid>
                    <Button Content="구성"
                            Click="ApplyLayout_Click"
                            HorizontalAlignment="Stretch"
                            Margin="0,0,0,8" />

                    <TextBlock x:Name="LayoutSummaryText"
                               Foreground="#55514A"
                               Margin="0,0,0,0" />
                    </StackPanel>
                    </StackPanel>
                    </Border>

                    <Border x:Name="PanelSectionBorder" Style="{StaticResource InspectorSection}">
                    <StackPanel>
                    <TextBlock Text="칸" Style="{StaticResource SectionHeader}" />

                    <ListBox x:Name="PanelListBox"
                             MinHeight="100"
                             MaxHeight="170"
                             SelectionChanged="PanelListBox_SelectionChanged"
                             MouseDoubleClick="PanelListBox_MouseDoubleClick"
                             PreviewKeyDown="PanelListBox_PreviewKeyDown"
                             Margin="0,0,0,8">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <!-- 표시: 잠금 아이콘 + 이름(비어 있으면 "N번 칸"). 편집 중이면 숨김. -->
                                    <TextBlock Text="{Binding DisplayText}">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <!-- 편집: 더블클릭/F2 시 표시되는 입력칸(기본 숨김은 Style로 — 로컬 값은 트리거를 덮어쓰므로). -->
                                    <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             Padding="2,0"
                                             IsVisibleChanged="PageRenameBox_IsVisibleChanged"
                                             KeyDown="PanelRenameBox_KeyDown"
                                             LostFocus="PanelRenameBox_LostFocus">
                                        <TextBox.Style>
                                            <Style TargetType="TextBox">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBox.Style>
                                    </TextBox>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <UniformGrid Rows="1" Columns="4" Margin="0,0,0,12">
                        <Button Content="추가"
                                Click="AddPanel_Click"
                                Margin="0,0,4,0" />
                        <Button Content="삭제"
                                Click="DeletePanel_Click"
                                Margin="0,0,4,0" />
                        <Button Content="위로"
                                Click="MovePanelUp_Click"
                                Margin="0,0,4,0" />
                        <Button Content="아래로"
                                Click="MovePanelDown_Click"
                                Margin="0" />
                    </UniformGrid>
                    <!-- 칸이 선택되지 않으면 아래 옵션을 숨긴다(리스트만 표시). -->
                    <StackPanel x:Name="PanelEditControls" Visibility="Collapsed">
                    <CheckBox x:Name="PanelLockCheckBox"
                              Content="잠금 (선택·이동 막기)"
                              IsChecked="False"
                              Checked="PanelLockCheckBox_Changed"
                              Unchecked="PanelLockCheckBox_Changed"
                              Margin="0,0,0,8" />
                    <CheckBox x:Name="PanelCornerModeCheckBox"
                              Content="모서리 조절 (사변형)"
                              IsChecked="False"
                              Checked="PanelCornerModeCheckBox_Changed"
                              Unchecked="PanelCornerModeCheckBox_Changed"
                              Margin="0,0,0,12" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBlock Text="배경색"
                                   Width="64"
                                   VerticalAlignment="Center"
                                   Foreground="#55514A" />
                        <ComboBox x:Name="PanelBackgroundColorComboBox"
                                  Width="130"
                                  SelectionChanged="PanelBackgroundColorComboBox_SelectionChanged" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="테두리색"
                                   Width="64"
                                   VerticalAlignment="Center"
                                   Foreground="#55514A" />
                        <ComboBox x:Name="PanelBorderColorComboBox"
                                  Width="130"
                                  SelectionChanged="PanelBorderColorComboBox_SelectionChanged" />
                    </StackPanel>
                    </StackPanel>
                    </StackPanel>
                    </Border>

                    <Border x:Name="ImageSectionBorder" Style="{StaticResource InspectorSection}" Visibility="Collapsed">
                    <StackPanel>
                    <TextBlock Text="이미지" Style="{StaticResource SectionHeader}" />
                    <ListBox x:Name="ImageListBox"
                             MinHeight="86"
                             MaxHeight="150"
                             SelectionChanged="ImageListBox_SelectionChanged"
                             Margin="0,0,0,8" />
                    <UniformGrid Rows="1" Columns="4" Margin="0,0,0,18">
                        <Button Content="추가"
                                Click="AddImageToPanel_Click"
                                Margin="0,0,4,0" />
                        <Button Content="삭제"
                                Click="ClearPanelImage_Click"
                                Margin="0,0,4,0" />
                        <Button Content="위로"
                                Click="MoveImageUp_Click"
                                Margin="0,0,4,0" />
                        <Button Content="아래로"
                                Click="MoveImageDown_Click"
                                Margin="0" />
                    </UniformGrid>
                    <!-- 이미지가 선택되지 않으면 아래 옵션을 숨긴다(리스트만 표시). -->
                    <StackPanel x:Name="ImageEditControls" Visibility="Collapsed">
                    <CheckBox x:Name="ImageLockCheckBox"
                              Content="잠금 (선택·이동 막기)"
                              IsChecked="False"
                              Checked="ImageLockCheckBox_Changed"
                              Unchecked="ImageLockCheckBox_Changed"
                              Margin="0,0,0,8" />
                    <CheckBox x:Name="ImageCropCheckBox"
                              Content="칸 경계에서 자르기"
                              IsChecked="True"
                              Checked="ImageCropCheckBox_Changed"
                              Unchecked="ImageCropCheckBox_Changed"
                              Margin="0,0,0,8" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,18">
                        <TextBlock Text="기준점"
                                   Width="64"
                                   VerticalAlignment="Center"
                                   Foreground="#55514A"
                                   Background="Transparent"
                                   ToolTip="X: 0=좌 1=우 / Y: 0=하 1=상 (칸 크기 조절 시 따라갈 위치)" />
                        <TextBox x:Name="ImagePivotXBox" Width="44" Text="0" TextChanged="ImagePivotBox_Changed" />
                        <TextBlock Text="," VerticalAlignment="Center" Margin="4,0" Foreground="#55514A" />
                        <TextBox x:Name="ImagePivotYBox" Width="44" Text="1" TextChanged="ImagePivotBox_Changed" />
                    </StackPanel>

                    <Border Style="{StaticResource InspectorGroup}">
                    <StackPanel>
                    <TextBlock Text="색 그라데이션" Style="{StaticResource GroupHeader}" />
                    <TextBlock Text="방향"
                               Foreground="#55514A"
                               Margin="0,0,0,4" />
                    <ComboBox x:Name="ImageGradientComboBox"
                              SelectionChanged="ImageGradientComboBox_SelectionChanged"
                              Margin="0,0,0,10">
                        <ComboBoxItem Content="없음" Tag="None" IsSelected="True" />
                        <ComboBoxItem Content="위" Tag="Top" />
                        <ComboBoxItem Content="아래" Tag="Bottom" />
                        <ComboBoxItem Content="왼쪽" Tag="Left" />
                        <ComboBoxItem Content="오른쪽" Tag="Right" />
                    </ComboBox>

                    <TextBlock x:Name="ImageGradientColorLabel"
                               Text="색 (투명 선택 시 이미지가 사라짐)"
                               Foreground="#55514A"
                               Margin="0,0,0,2" />
                    <ComboBox x:Name="ImageGradientColorComboBox"
                              SelectionChanged="ImageGradientColorComboBox_SelectionChanged"
                              Margin="0,0,0,10" />

                    <TextBlock x:Name="ImageGradientStartText"
                               Foreground="#55514A"
                               Margin="0,0,0,2" />
                    <Slider x:Name="ImageGradientStartSlider"
                            Minimum="0"
                            Maximum="100"
                            Value="40"
                            ValueChanged="ImageGradientSlider_ValueChanged"
                            Margin="0,0,0,10" />

                    <TextBlock x:Name="ImageGradientEndText"
                               Foreground="#55514A"
                               Margin="0,0,0,2" />
                    <Slider x:Name="ImageGradientEndSlider"
                            Minimum="0"
                            Maximum="100"
                            Value="60"
                            ValueChanged="ImageGradientSlider_ValueChanged"
                            Margin="0,0,0,2" />
                    </StackPanel>
                    </Border>

                    <Border x:Name="ImageOutputGroup" Style="{StaticResource InspectorGroup}" Visibility="Collapsed">
                    <StackPanel>
                    <TextBlock Text="출력 (움직이는 이미지)" Style="{StaticResource GroupHeader}" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBlock Text="시간(초)" Width="64" VerticalAlignment="Center" Foreground="#55514A" />
                        <TextBox x:Name="ImageOutputDurationBox" Width="64" TextChanged="ImageOutputBox_Changed" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBlock Text="FPS" Width="64" VerticalAlignment="Center" Foreground="#55514A" />
                        <TextBox x:Name="ImageOutputFpsBox" Width="64" TextChanged="ImageOutputBox_Changed" />
                    </StackPanel>
                    <TextBlock Text="시간=한 바퀴 길이, FPS=내보내기 부드러움. 편집 미리보기·WebP 내보내기에 함께 적용됩니다."
                               Foreground="#777268" TextWrapping="Wrap" Margin="0,2,0,0" />
                    </StackPanel>
                    </Border>
                    </StackPanel>

                    <StackPanel Visibility="Collapsed">
                        <TextBlock Text="칸 위치와 크기"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextBrush}"
                                   Margin="0,0,0,8" />
                        <TextBlock x:Name="PanelXText" Foreground="#55514A" />
                        <Slider x:Name="PanelXSlider"
                                Minimum="0"
                                Maximum="760"
                                Value="40"
                                ValueChanged="PanelSlider_ValueChanged"
                                Margin="0,4,0,8" />
                        <TextBlock x:Name="PanelYText" Foreground="#55514A" />
                        <Slider x:Name="PanelYSlider"
                                Minimum="0"
                                Maximum="1040"
                                Value="40"
                                ValueChanged="PanelSlider_ValueChanged"
                                Margin="0,4,0,8" />
                        <TextBlock x:Name="PanelWidthText" Foreground="#55514A" />
                        <Slider x:Name="PanelWidthSlider"
                                Minimum="120"
                                Maximum="760"
                                Value="340"
                                ValueChanged="PanelSlider_ValueChanged"
                                Margin="0,4,0,8" />
                        <TextBlock x:Name="PanelHeightText" Foreground="#55514A" />
                        <Slider x:Name="PanelHeightSlider"
                                Minimum="120"
                                Maximum="520"
                                Value="300"
                                ValueChanged="PanelSlider_ValueChanged"
                                Margin="0,4,0,18" />
                    </StackPanel>
                    </StackPanel>
                    </Border>

                    <Border x:Name="BubbleSectionBorder" Style="{StaticResource InspectorSection}" Visibility="Collapsed">
                    <StackPanel>
                    <TextBlock Text="말풍선" Style="{StaticResource SectionHeader}" />
                    <ListBox x:Name="BubbleListBox"
                             MinHeight="86"
                             MaxHeight="150"
                             SelectionChanged="BubbleListBox_SelectionChanged"
                             Margin="0,0,0,8" />
                    <UniformGrid Rows="1" Columns="4" Margin="0,0,0,12">
                        <Button Content="추가"
                                Click="AddBubble_Click"
                                Margin="0,0,4,0" />
                        <Button Content="삭제"
                                Click="DeleteBubble_Click"
                                Margin="0,0,4,0" />
                        <Button Content="위로"
                                Click="MoveBubbleUp_Click"
                                Margin="0,0,4,0" />
                        <Button Content="아래로"
                                Click="MoveBubbleDown_Click"
                                Margin="0" />
                    </UniformGrid>
                    <!-- 말풍선이 선택되지 않으면 아래 편집 옵션 전체를 숨긴다(리스트만 표시). -->
                    <StackPanel x:Name="BubbleEditControls" Visibility="Collapsed">
                    <CheckBox x:Name="BubbleLockCheckBox"
                              Content="잠금 (선택·이동 막기)"
                              IsChecked="False"
                              Checked="BubbleLockCheckBox_Changed"
                              Unchecked="BubbleLockCheckBox_Changed"
                              Margin="0,0,0,8" />
                    <CheckBox x:Name="BubbleCropCheckBox"
                              Content="칸 경계에서 자르기"
                              IsChecked="True"
                              Checked="BubbleCropCheckBox_Changed"
                              Unchecked="BubbleCropCheckBox_Changed"
                              Margin="0,0,0,8" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="기준점"
                                   Width="64"
                                   VerticalAlignment="Center"
                                   Foreground="#55514A"
                                   Background="Transparent"
                                   ToolTip="X: 0=좌 1=우 / Y: 0=하 1=상 (칸 크기 조절 시 따라갈 위치)" />
                        <TextBox x:Name="BubblePivotXBox" Width="44" Text="0" TextChanged="BubblePivotBox_Changed" />
                        <TextBlock Text="," VerticalAlignment="Center" Margin="4,0" Foreground="#55514A" />
                        <TextBox x:Name="BubblePivotYBox" Width="44" Text="1" TextChanged="BubblePivotBox_Changed" />
                    </StackPanel>
                    <Border Style="{StaticResource InspectorGroup}">
                    <StackPanel>
                    <TextBlock Text="텍스트" Style="{StaticResource GroupHeader}" />

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBlock Text="글자 크기"
                                   Width="64"
                                   VerticalAlignment="Center"
                                   Foreground="#55514A" />
                        <TextBox x:Name="BubbleFontBox"
                                 Width="130"
                                 Text="18"
                                 Padding="6,2"
                                 VerticalContentAlignment="Center"
                                 TextChanged="BubbleFontBox_TextChanged" />
                        <TextBlock Text="px"
                                   VerticalAlignment="Center"
                                   Margin="6,0,0,0"
                                   Foreground="#55514A" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBlock Text="줄간격"
                                   Width="64"
                                   VerticalAlignment="Center"
                                   Foreground="#55514A" />
                        <TextBox x:Name="BubbleLineHeightBox"
                                 Width="130"
                                 Text="0"
                                 Padding="6,2"
                                 VerticalContentAlignment="Center"
                                 ToolTip="0이면 자동"
                                 TextChanged="BubbleLineHeightBox_TextChanged" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBlock Text="가로 맞춤"
                                   Width="64"
                                   VerticalAlignment="Center"
                                   Foreground="#55514A" />
                        <ComboBox x:Name="BubbleAlignmentComboBox"
                                  Width="130"
                                  SelectionChanged="BubbleAlignmentComboBox_SelectionChanged">
                            <ComboBoxItem Content="왼쪽" Tag="Left" />
                            <ComboBoxItem Content="가운데" Tag="Center" IsSelected="True" />
                            <ComboBoxItem Content="오른쪽" Tag="Right" />
                            <ComboBoxItem Content="양쪽" Tag="Justify" />
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBlock Text="세로 맞춤"
                                   Width="64"
                                   VerticalAlignment="Center"
                                   Foreground="#55514A" />
                        <ComboBox x:Name="BubbleVAlignComboBox"
                                  Width="130"
                                  SelectionChanged="BubbleVAlignComboBox_SelectionChanged">
                            <ComboBoxItem Content="위" Tag="Top" />
                            <ComboBoxItem Content="가운데" Tag="Center" IsSelected="True" />
                            <ComboBoxItem Content="아래" Tag="Bottom" />
                        </ComboBox>
                    </StackPanel>

                    <TextBlock Text="말풍선 텍스트"
                               Foreground="#55514A"
                               Margin="0,0,0,6" />
                    <TextBox x:Name="SelectedBubbleTextBox"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             MinHeight="74"
                             MaxHeight="200"
                             VerticalScrollBarVisibility="Auto"
                             Padding="8"
                             IsInactiveSelectionHighlightEnabled="True"
                             ToolTip="구간을 선택한 뒤 글자색·아웃라인색·글꼴을 바꾸면 그 부분만 적용됩니다(선택이 없으면 전체)."
                             TextChanged="SelectedBubbleTextBox_TextChanged"
                             Margin="0,0,0,12" />

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBlock Text="글꼴"
                                   Width="64"
                                   VerticalAlignment="Center"
                                   Foreground="#55514A" />
                        <ComboBox x:Name="BubbleFontFamilyComboBox"
                                  Width="130"
                                  MaxDropDownHeight="320"
                                  SelectionChanged="BubbleFontFamilyComboBox_SelectionChanged" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBlock Text="글자색"
                                   Width="64"
                                   VerticalAlignment="Center"
                                   Foreground="#55514A" />
                        <ComboBox x:Name="BubbleFillColorComboBox"
                                  Width="130"
                                  SelectionChanged="BubbleFillColorComboBox_SelectionChanged" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBlock Text="아웃라인색"
                                   Width="64"
                                   VerticalAlignment="Center"
                                   Foreground="#55514A" />
                        <ComboBox x:Name="BubbleStrokeColorComboBox"
                                  Width="130"
                                  SelectionChanged="BubbleStrokeColorComboBox_SelectionChanged" />
                    </StackPanel>
                    <TextBlock Text="텍스트의 일부를 선택한 뒤 글꼴·글자색·아웃라인색을 바꾸면 그 부분에만 적용됩니다(선택이 없으면 전체)."
                               TextWrapping="Wrap" FontSize="11" Foreground="#8A8479" Margin="0,2,0,0" />
                    </StackPanel>
                    </Border>

                    <Border Style="{StaticResource InspectorGroup}">
                    <StackPanel>
                    <TextBlock Text="변형 · 배경 · 테두리" Style="{StaticResource GroupHeader}" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBlock Text="변형"
                                   Width="64"
                                   VerticalAlignment="Center"
                                   Foreground="#55514A"
                                   ToolTip="네 모서리를 끌어 사변형으로 일그러뜨립니다." />
                        <CheckBox x:Name="BubbleWarpShapeCheckBox" Content="테두리"
                                  VerticalAlignment="Center" Margin="0,0,14,0"
                                  Checked="BubbleWarpCheckBox_Changed" Unchecked="BubbleWarpCheckBox_Changed" />
                        <CheckBox x:Name="BubbleWarpTextCheckBox" Content="글자"
                                  VerticalAlignment="Center"
                                  Checked="BubbleWarpCheckBox_Changed" Unchecked="BubbleWarpCheckBox_Changed" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBlock Text="배경색"
                                   Width="64"
                                   VerticalAlignment="Center"
                                   Foreground="#55514A" />
                        <ComboBox x:Name="BubbleBackgroundColorComboBox"
                                  Width="130"
                                  SelectionChanged="BubbleBackgroundColorComboBox_SelectionChanged" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBlock Text="테두리색"
                                   Width="64"
                                   VerticalAlignment="Center"
                                   Foreground="#55514A" />
                        <ComboBox x:Name="BubbleBorderColorComboBox"
                                  Width="130"
                                  SelectionChanged="BubbleBorderColorComboBox_SelectionChanged" />
                    </StackPanel>
                    </StackPanel>
                    </Border>

                    <Border Style="{StaticResource InspectorGroup}">
                    <StackPanel>
                    <TextBlock Text="모양" Style="{StaticResource GroupHeader}" />
                    <TextBlock Text="말풍선 모양"
                               Foreground="#55514A"
                               Margin="0,0,0,6" />
                    <ComboBox x:Name="BubbleShapeComboBox"
                              SelectionChanged="BubbleShapeComboBox_SelectionChanged"
                              Margin="0,0,0,12">
                        <ComboBoxItem Content="원형/사각" Tag="RoundRect" IsSelected="True" />
                        <ComboBoxItem Content="구름/폭발" Tag="CloudExplosion" />
                        <ComboBoxItem Content="플래시" Tag="Flash" />
                        <ComboBoxItem Content="집중선" Tag="ConcentrationLines" />
                        <ComboBoxItem Content="속도선" Tag="EffectLines" />
                        <ComboBoxItem Content="테두리 없음" Tag="None" />
                    </ComboBox>

                    <TextBlock x:Name="BubbleShapeStrengthText"
                               Foreground="#55514A"
                               Margin="0,0,0,2" />
                    <Slider x:Name="BubbleShapeStrengthSlider"
                            Minimum="0"
                            Maximum="100"
                            Value="0"
                            ValueChanged="BubbleShapeStrengthSlider_ValueChanged"
                            Margin="0,0,0,10" />

                    <TextBlock x:Name="BubbleTextRotationText"
                               Foreground="#55514A"
                               Margin="0,0,0,2" />
                    <Slider x:Name="BubbleTextRotationSlider"
                            Minimum="0"
                            Maximum="360"
                            Value="0"
                            ValueChanged="BubbleTextRotationSlider_ValueChanged"
                            Margin="0,0,0,10" />

                    <TextBlock x:Name="BubbleShapeCountText"
                               Foreground="#55514A"
                               Margin="0,0,0,2" />
                    <Slider x:Name="BubbleShapeCountSlider"
                            Minimum="3"
                            Maximum="100"
                            Value="9"
                            IsSnapToTickEnabled="True"
                            TickFrequency="1"
                            ValueChanged="BubbleShapeCountSlider_ValueChanged"
                            Margin="0,0,0,12" />

                    <TextBlock x:Name="BubbleShapeIrregularityText"
                               Foreground="#55514A"
                               Margin="0,0,0,2" />
                    <Slider x:Name="BubbleShapeIrregularitySlider"
                            Minimum="0"
                            Maximum="100"
                            Value="50"
                            ValueChanged="BubbleShapeIrregularitySlider_ValueChanged"
                            Margin="0,0,0,12" />

                    <TextBlock x:Name="BubbleShapeWidthVarText"
                               Foreground="#55514A"
                               Margin="0,0,0,2" />
                    <Slider x:Name="BubbleShapeWidthVarSlider"
                            Minimum="0"
                            Maximum="100"
                            Value="0"
                            ValueChanged="BubbleShapeWidthVarSlider_ValueChanged"
                            Margin="0,0,0,2" />

                    <CheckBox x:Name="BubbleLineFadeBothSidesCheckBox"
                              Content="양쪽 페이드 (양 끝 투명)"
                              Visibility="Collapsed"
                              Checked="BubbleLineFadeBothSides_Changed"
                              Unchecked="BubbleLineFadeBothSides_Changed"
                              Margin="0,2,0,2" />
                    </StackPanel>
                    </Border>

                    <StackPanel Visibility="Collapsed">
                        <TextBlock x:Name="BubbleWidthText" Foreground="#55514A" />
                        <Slider x:Name="BubbleWidthSlider"
                                Minimum="90"
                                Maximum="6000"
                                Value="170"
                                ValueChanged="BubbleSlider_ValueChanged"
                                Margin="0,4,0,8" />
                        <TextBlock x:Name="BubbleHeightText" Foreground="#55514A" />
                        <Slider x:Name="BubbleHeightSlider"
                                Minimum="56"
                                Maximum="6000"
                                Value="100"
                                ValueChanged="BubbleSlider_ValueChanged"
                                Margin="0,4,0,8" />
                    </StackPanel>
                    <StackPanel Visibility="Collapsed">
                        <TextBlock x:Name="BubbleXText" Foreground="#55514A" />
                        <Slider x:Name="BubbleXSlider"
                                Minimum="-240"
                                Maximum="700"
                                Value="30"
                                ValueChanged="BubbleSlider_ValueChanged"
                                Margin="0,4,0,8" />
                        <TextBlock x:Name="BubbleYText" Foreground="#55514A" />
                        <Slider x:Name="BubbleYSlider"
                                Minimum="-180"
                                Maximum="420"
                                Value="30"
                                ValueChanged="BubbleSlider_ValueChanged"
                                Margin="0,4,0,14" />
                    </StackPanel>

                    <Border x:Name="BubbleTailSectionBorder" Style="{StaticResource InspectorGroup}">
                    <StackPanel>
                    <TextBlock Text="꼬리" Style="{StaticResource GroupHeader}" />
                    <ListBox x:Name="BubbleTailListBox"
                             MinHeight="72"
                             MaxHeight="130"
                             SelectionChanged="BubbleTailListBox_SelectionChanged"
                             Margin="0,0,0,8" />
                    <UniformGrid Rows="1" Columns="4" Margin="0,0,0,8">
                        <Button Content="추가"
                                Click="AddBubbleTail_Click"
                                Margin="0,0,4,0" />
                        <Button Content="삭제"
                                Click="DeleteBubbleTail_Click"
                                Margin="0,0,4,0" />
                        <Button Content="위로"
                                Click="MoveBubbleTailUp_Click"
                                Margin="0,0,4,0" />
                        <Button Content="아래로"
                                Click="MoveBubbleTailDown_Click"
                                Margin="0" />
                    </UniformGrid>
                    <CheckBox x:Name="BubbleTailInwardCheckBox"
                              Content="꼬리 안으로 깎기"
                              IsChecked="False"
                              Checked="BubbleTailInwardCheckBox_Changed"
                              Unchecked="BubbleTailInwardCheckBox_Changed"
                              Margin="0,0,0,8" />
                    <CheckBox x:Name="BubbleThoughtTailCheckBox"
                              Content="생각 꼬리 (원형 3개)"
                              IsChecked="False"
                              Checked="BubbleThoughtTail_Changed"
                              Unchecked="BubbleThoughtTail_Changed"
                              Margin="0,0,0,12" />

                    <StackPanel Visibility="Collapsed">
                        <TextBlock x:Name="BubbleTailXText" Foreground="#55514A" />
                        <Slider x:Name="BubbleTailXSlider"
                                Minimum="-240"
                                Maximum="420"
                                Value="130"
                                ValueChanged="BubbleTailSlider_ValueChanged"
                                Margin="0,4,0,8" />
                        <TextBlock x:Name="BubbleTailYText" Foreground="#55514A" />
                        <Slider x:Name="BubbleTailYSlider"
                                Minimum="-180"
                                Maximum="420"
                                Value="130"
                                ValueChanged="BubbleTailSlider_ValueChanged"
                                Margin="0,4,0,8" />
                    </StackPanel>
                    <TextBlock x:Name="BubbleTailWidthText" Foreground="#55514A" />
                    <Slider x:Name="BubbleTailWidthSlider"
                            Minimum="8"
                            Maximum="80"
                            Value="15"
                            ValueChanged="BubbleTailSlider_ValueChanged"
                            Margin="0,4,0,14" />
                    <TextBlock Text="꼬리는 세 점(시작·중간·끝) 핸들로 곡선을 조절합니다. 주황 핸들(중간)을 끌면 곡선이 휩니다."
                               TextWrapping="Wrap"
                               Foreground="#77736A"
                               Margin="0,2,0,0" />
                    </StackPanel>
                    </Border>
                    </StackPanel>
                    </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- 인스펙터 우측 가장자리를 드래그해 폭을 조절한다(현재/다음 열을 함께 리사이즈). -->
        <GridSplitter Grid.Column="0"
                      Width="6"
                      HorizontalAlignment="Right"
                      VerticalAlignment="Stretch"
                      Background="Transparent"
                      ResizeBehavior="CurrentAndNext"
                      ResizeDirection="Columns"
                      Cursor="SizeWE" />

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0"
                          x:Name="PageScrollViewer"
                          HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto"
                          Padding="28"
                          Background="Transparent"
                          SizeChanged="PageScrollViewer_SizeChanged"
                          PreviewMouseLeftButtonDown="PageScrollViewer_PreviewMouseLeftButtonDown"
                          PreviewMouseWheel="PageScrollViewer_PreviewMouseWheel">
                <Border x:Name="PageFrame"
                        Background="#FFFFFF"
                        BorderBrush="#D7D2C8"
                        BorderThickness="1"
                        Padding="0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                    <!-- PageSurface/PageOverlay는 클리핑하지 않아 선택 UI(핸들·박스)가 페이지 밖에서도 보인다.
                         실제 콘텐츠(칸) 클리핑은 PanelCanvas가 담당한다. -->
                    <Grid x:Name="PageSurface"
                          Width="832"
                          Height="1216"
                          ClipToBounds="False"
                          Background="#FFFFFF"
                          PreviewMouseMove="PageSurface_PreviewMouseMove"
                          MouseLeave="PageSurface_MouseLeave">
                        <Canvas x:Name="PanelCanvas"
                                Width="832"
                                Height="1216"
                                ClipToBounds="True" />
                        <Canvas x:Name="PageOverlay"
                                Width="832"
                                Height="1216"
                                ClipToBounds="False" />
                    </Grid>
                </Border>
            </ScrollViewer>

            <Button Grid.Row="0"
                    x:Name="InspectorToggleButton"
                    Content="◀"
                    Click="ToggleInspector_Click"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Width="28"
                    Height="28"
                    MinHeight="0"
                    Padding="0"
                    FontSize="13"
                    Margin="8"
                    ToolTip="인스펙터 접기/펴기" />

            <Border Grid.Row="1"
                    Background="#FFFFFF"
                    BorderBrush="#DDD8CE"
                    BorderThickness="0,1,0,0"
                    Padding="14,10">
                <DockPanel>
                    <TextBlock x:Name="StatusText"
                               DockPanel.Dock="Left"
                               VerticalAlignment="Center"
                               Foreground="#55514A"
                               Text="준비됨" />
                    <TextBlock x:Name="PageIndicatorText"
                               DockPanel.Dock="Right"
                               HorizontalAlignment="Right"
                               VerticalAlignment="Center"
                               Foreground="#55514A"
                               FontWeight="SemiBold"
                               Text="페이지 1 / 1" />
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
    </DockPanel>
</Window>
